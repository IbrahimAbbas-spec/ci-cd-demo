# ci-cd-jenkins-demo

A minimal Flask application with a Jenkins CI/CD pipeline (Build → Test → Deploy as a Docker container).

> Work in progress — full documentation will be added once the pipeline runs end-to-end.
